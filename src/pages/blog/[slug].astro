---
import BlogPostPage from "../../components/pages/BlogPostPage.astro";
import { makeBlogPaths, type BlogEntry } from "../../lib/blog";

export const getStaticPaths = makeBlogPaths("en");

interface Props {
  post: BlogEntry;
}

const { post } = Astro.props;
---

<BlogPostPage post={post} />
